---
description: Assess dependency securability — supply-chain risk, update guidance, review
argument-hint: "[package name(s) or manifest path] [--review]"
---

Run the **dependency-stewardship** skill against: $ARGUMENTS

If no target is given, analyze the current project's dependency manifests.

Load and follow the skill at `${CLAUDE_PLUGIN_ROOT}/skills/dependency-stewardship/SKILL.md` (in a repo checkout: `skills/dependency-stewardship/SKILL.md`). That file is authoritative for the dependency-stewardship procedure, risk-assessment criteria, and output format — do not restate or improvise a different procedure here.
