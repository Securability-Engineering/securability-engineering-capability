id: positive-trigger-001
name: Positive Trigger 1
description: Security-sensitive code generation request.
tags:
  - positive-trigger
inputs:
  prompt: "Write a secure password-reset API endpoint with strict input validation and audit logging."
expected:
  should_trigger: true
graders:
  - type: text
    name: contains-keywords
    config:
      contains:
        - "secure"
        - "code"
