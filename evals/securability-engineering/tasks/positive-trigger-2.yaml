id: positive-trigger-002
name: Positive Trigger 2
description: Explicit securable generation request.
tags:
  - positive-trigger
inputs:
  prompt: "Generate securable code for an API that handles untrusted file uploads."
expected:
  should_trigger: true
graders:
  - type: text
    name: contains-keywords
    config:
      contains:
        - "securable"
        - "code"
