id: positive-trigger-002
name: Positive Trigger 2
description: Explicit remediation request.
tags:
  - positive-trigger
inputs:
  prompt: "Remediate the triaged vulnerability with a review-ready patch."
expected:
  should_trigger: true
graders:
  - type: text
    name: contains-keywords
    config:
      contains:
        - "remediate"
        - "patch"
