id: positive-trigger-001
name: Positive Trigger 1
description: Confirmed security finding remediation request.
tags:
  - positive-trigger
inputs:
  prompt: "Fix this confirmed security finding and include a regression test and Securability Note."
expected:
  should_trigger: true
graders:
  - type: text
    name: contains-keywords
    config:
      contains:
        - "security"
        - "finding"
