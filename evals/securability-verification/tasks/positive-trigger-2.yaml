id: positive-trigger-002
name: Positive Trigger 2
description: Deployment validation request.
tags:
  - positive-trigger
inputs:
  prompt: "Check the deployment configuration and produce a release posture summary with evidence."
expected:
  should_trigger: true
graders:
  - type: text
    name: contains-keywords
    config:
      contains:
        - "deployment"
        - "verification"
