id: positive-trigger-001
name: Positive Trigger 1
description: Boundary verification request.
tags:
  - positive-trigger
inputs:
  prompt: "Generate boundary contract tests and verify our security requirements before release."
expected:
  should_trigger: true
graders:
  - type: text
    name: contains-keywords
    config:
      contains:
        - "verify"
        - "requirements"
